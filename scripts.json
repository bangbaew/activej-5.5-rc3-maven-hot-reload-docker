{
    "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
    "scripts": {
        "start": {
            "cmd": "mvn install -DskipTests && mvn exec:java -Dexec.mainClass=org.example.WebApp",
            "desc": "run main class"
        }
    },
    "watcher": {
        "exts": [
            "java"
        ]
    }
}